This folder contains the three models selected to satisfy the
project requirement:

1. MLPClassifier (Neural baseline)
2. BiLSTM (PyTorch, built from scratch)
3. BERT (bert-base-uncased, pretrained transformer)

These notebooks are used for final evaluation, reporting,
and viva presentation.

All other experiments and alternative models are available
in `notebooks/experiments/`.
